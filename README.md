A Decision Tree is a supervised machine learning algorithm used for both classification and regression tasks. It predicts outcomes by splitting data into branches based on feature conditions until a final decision is reached.

It is commonly implemented using scikit-learn.
